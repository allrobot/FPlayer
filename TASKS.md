# AI 阶段任务

每个任务设计为一个独立 AI 会话可承担的上下文。默认限制约 8 到 12 个主要源码文件；开始前读取 `AGENTS.md`、`PROGRESS.md` 和任务指定文档，完成后更新 `PROGRESS.md`。

## P0 工程基线

### T00 可编译骨架与规格冻结（当前）

- 输入：`SPEC.md`、`docs/architecture/*`、`docs/security/test-device-policy.md`
- 修改：根 Gradle、`app/`、`core/*` 契约、文档
- 验收：`gradlew :app:assembleDebug`、`git diff --check`
- 退出：不实现外设连接，不安装 APK

## P1 Native 播放内核

### T01 许可证矩阵与构建锁定

- 输入：`REF_MPV_ANDROID`、`REF_MPV`、`REF_FFMPEG`、`REF_LIBASS`、`REF_LIBPLACEBO`、`REF_DAV1D`
- 产出：依赖版本锁、编译开关、ABI、GPL/LGPL 选择、NOTICE 来源表
- 验收：每个二进制依赖都能追溯仓库 commit、配置和许可证
- 禁止：复制源码进产品仓库、修改参考仓库

### T02 Ubuntu arm64-v8a libmpv 构建

- 输入：T01 锁定文件、`LINUX_BUILDER`、`NDK_R29`
- 修改：`native-build/` 脚本和文档，不改上游仓库
- 验收：干净目录可重复产出 arm64-v8a 库、头文件、配置清单和校验和
- 退出：磁盘不足时记录精确空间需求，不降级或删除用户文件

### T03 libmpv Android 适配器

- 输入：`core/player-api/`、T02 产物、`REF_MPV_ANDROID`
- 修改：新增 `core/player-mpv/`，少量 app 装配
- 验收：合成视频 prepare/play/pause/seek/speed/release 单测或仪器测试
- 禁止：在 UI 中直接调用 JNI/libmpv

## P2 Funscript 与媒体时钟

### T04 Funscript 严格解析和同名匹配

- 输入：`core/script/`、公开格式样本、`REF_MULTI_FUN_PLAYER`、`REF_DORO_PLAYER`
- 修改：解析器、命名规范、错误模型、单/多轴测试向量
- 验收：乱序、重复时间、越界 pos、空脚本、多轴冲突全部有确定结果

### T05 插值、偏移和同步调度器

- 输入：T04、`core/player-api/`、`core/device/`
- 修改：纯 Kotlin 调度器和确定性测试时钟
- 验收：seek、pause、speed、loop、切片和播放结束时命令流可复现
- 禁止：调度线程直接访问具体串口/蓝牙 API

### T06 热力图数据降采样

- 输入：T04 规范、`docs/ui/interaction-spec.md`
- 修改：脚本可视化模型和降采样算法，不实现完整 Feed
- 验收：长脚本内存有界，缩放/拖动时保留极值和时间对应关系

## P3 TCode 核心与安全层

### T07 TCode 编码、版本和轴注册表

- 输入：`REF_TCODE_FIRMWARE` 的 TCode 0.2/0.3/0.4 实现
- 修改：`core/device/` 编码器、轴描述、版本协商、测试向量
- 验收：已知命令逐字节匹配固件语义；未知轴和越界值被拒绝
- 禁止：修改或烧录固件

### T08 设备调度、限幅和统一急停

- 输入：T05、T07、`docs/architecture/device-layer.md`
- 修改：有界队列、钳制、速率限制、过期丢弃、stop 状态机
- 验收：断连、异常、队列拥塞和 release 都只产生一次可证明的停止序列

### T09 网络传输

- 输入：T07/T08、`REF_AYVA_WS_HUB`、固件 TCP/UDP/WebSocket handler
- 修改：TCP、UDP、raw WebSocket 三个 transport
- 验收：loopback/模拟服务器的连接、重连、背压、断连和停止测试

### T10 USB 串口传输

- 输入：T07/T08、Android USB Host API、选定串口库
- 修改：权限、设备过滤、串口参数、读写循环和诊断
- 验收：模拟/loopback 后才允许在低幅配置连接 `TEST_OSR_DEVICE`
- 禁止：固件烧录、持久设备配置写入

### T11 BLE 传输

- 输入：固件 BLE service/characteristic、Android 12+ 权限模型
- 修改：扫描过滤、连接、MTU/分包、订阅、重连
- 验收：权限拒绝、蓝牙关闭、超时和 GATT 133 类失败均可恢复并急停

### T12 经典蓝牙 SPP

- 输入：固件 Bluetooth handler、Android RFCOMM API
- 修改：已配对设备选择、socket 生命周期、流写入和重连
- 验收：不依赖 Intiface；断连清队列；不自动配对未知设备

### T13 设备配置 UI

- 输入：T08~T12、`feature/device/`
- 修改：发现、连接、轴限制、测试动作、诊断 UI
- 验收：默认测试幅度受限；危险动作二次确认；所有状态可访问且文本不溢出

## P4 索引与大型媒体库

### T14 Room 模式与迁移基线

- 输入：`docs/architecture/media-index.md`、`core/index/`
- 修改：实体、DAO、迁移、数据库测试
- 验收：媒体、脚本、来源、播放行为、物化顺序和扫描 generation 可一致提交

### T15 本地 SAF 增量扫描

- 输入：T14、Storage Access Framework
- 修改：持久授权、目录遍历、增删改检测、取消/恢复
- 验收：不请求 root；扫描中断不删除旧索引；2,000+ 合成条目性能基准

### T16 元数据和缩略图流水线

- 输入：T03、T15
- 修改：优先队列、元数据提取、缩略图缓存和延迟清理
- 验收：当前/相邻媒体优先；完整扫描成功前不清陈旧缩略图

### T17 排序与物化顺序

- 输入：T14/T15、`SortSpec`
- 修改：稳定排序、generation、增量失效和当前媒体重定位
- 验收：全部字段双向排序；同值次级键稳定；切换排序不切当前视频

### T18 SMB 来源与凭据封装

- 输入：T14/T17、选定 SMB 库
- 修改：连接配置、Keystore 凭据、增量扫描、断网缓存
- 验收：仓库/数据库/日志无明文凭据；离线仍可展示已索引元数据

## P5 前后台播放会话

### T19 播放会话和三槽预加载

- 输入：T03、T17、`feature/feed/`
- 修改：队列、上一/当前/下一、断点、循环/连播
- 验收：快速滑动、排序变更、失效媒体和解码错误不会串音或串脚本

### T20 Foreground Service、MediaSession 和通知

- 输入：T05/T08/T19、`docs/architecture/background-playback.md`
- 修改：服务、会话、通知动作、Activity 绑定
- 验收：锁屏循环；播放/暂停；停止设备；退出后台；服务销毁急停
- 禁止：无持续通知时后台驱动设备

### T21 生命周期和故障注入

- 输入：T20
- 修改：恢复策略、超时、崩溃/进程终止测试夹具
- 验收：Activity 重建不断流；任务划掉/force-stop 等系统限制有明确行为；任何错误不遗留设备队列

## P6 抖音式交互与控件

### T22 垂直 Feed 分页和手势仲裁

- 输入：T19、`docs/ui/interaction-spec.md`
- 修改：Feed 容器、吸附阈值、预加载信号、单击/纵滑/横滑仲裁
- 验收：未过阈值回弹，不停半屏；快速连续手势不出现双播放

### T23 播放叠层和清屏模式

- 输入：T22、UI 规格
- 修改：顶部工具带、右侧操作、左下信息、底部导航、清屏四控件
- 验收：手机/平板横竖屏无重叠；系统栏和安全区正确；按钮热区不漂移

### T24 进度条/Funscript 热力图交互

- 输入：T06、T23
- 修改：普通/热力图共享拖动状态机、时间浮层、触觉反馈
- 验收：播放/暂停/拖动视觉状态正确；两端反馈一次；拖动不触发 Feed 翻页

### T25 长按播放设置面板

- 输入：T23/T24
- 修改：竖屏 bottom sheet、横屏 side sheet、播放选项和区域冲突规则
- 验收：普通长按、边缘临时倍速、滑动和点击互斥；面板状态旋转后保持

### T26 自定义设置项编排

- 输入：T25
- 修改：已选/未选分区、加减、拖拽、边缘自动滚动和持久化
- 验收：拖拽白线位置准确；无卡片嵌套；键盘/无障碍可完成同一操作

### T27 网格/双列与播放位置跳转

- 输入：T17/T19/T22、`feature/library/`
- 修改：文件夹头部、视频/脚本切换、刚刚看过定位和进度标记
- 验收：从播放左滑进入并能精确回到当前项；底部首页返回规则一致

### T28 相册、抽屉、历史和搜索

- 输入：T17/T27
- 修改：三种相册视图、抽屉集合、排序/布局/搜索
- 验收：首页与相册共享排序；历史/喜欢/收藏/点踩查询一致；删除操作单独确认

## P7 推荐与质量

### T29 纯算法候选评分

- 输入：T14、`docs/architecture/recommendation.md`
- 修改：事件聚合、可解释特征、时间衰减、冷启动和单测
- 验收：同一数据和 seed 产生相同结果；后台循环权重低且饱和；点踩强降权

### T30 MMR 重排和探索位

- 输入：T29
- 修改：相似度、MMR、5%~10% 探索和解释输出
- 验收：不连续堆叠相似路径/标题；小库降级合理；用户可重置画像

### T31 性能、稳定性与真实设备验收

- 输入：全部已完成阶段、安全策略
- 修改：基准、宏基准、故障注入、测试报告；只修复验收发现的问题
- 验收：2,000+ 库、长时间后台循环、网络切换、锁屏、旋转、低内存和设备断连
- 真实设备：先 `TEST_TABLET` 应用私有边界，再低幅 `TEST_OSR_DEVICE`

### T32 发布与开源合规

- 输入：T01、最终依赖锁、全部测试报告
- 修改：NOTICE、源码归属、隐私说明、可复现构建、签名说明
- 验收：许可证矩阵无缺口；无凭据/真实媒体/本机绝对路径；发布包可从标签重建
