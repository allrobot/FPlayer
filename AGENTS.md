# AGENTS.md - FPlayer Android 协作规则

本文件约束所有在本仓库工作的 AI 会话。关键状态必须写入仓库，不能只存在于聊天记录。

## 1. 新会话恢复顺序

1. 完整读取 `AGENTS.md`。
2. 读取 `PROGRESS.md`。
3. 仅在需要目录入口时读取 `README.md`。
4. 读取 `PROGRESS.md` 明确指向的少量规格或源码。

禁止冷启动时递归读取全部文档、上游仓库、历史日志或生成物。

## 2. 当前产品边界

- Android 本地/SMB 视频播放器，交互参考短视频应用但不复制其品牌、素材或私有实现。
- 播放内核采用 libmpv；Android 侧原生实现 Funscript 同步和 TCode 设备控制。
- 首期设备传输：TCP、UDP、WebSocket、BLE、经典蓝牙 SPP、USB 串口。
- 后台播放是首期能力：锁屏后可循环媒体和脚本，但必须运行前台服务并提供显式停止设备入口。
- Intiface、Handy/Handyplug 暂不纳入首期。
- 推荐为可解释的确定性算法，不引入本地模型或云端画像。

## 3. 上下文外置化（强制）

- 跨会话仍需要的决定、事实、逻辑路径、风险、验证结果和下一步必须写入项目文件。
- 记录结论和证据指针，不记录逐句聊天或内部推理过程。
- 连续小修改合并为一次 checkpoint，避免频繁写盘和文件膨胀。
- `PROGRESS.md` 控制在 200 行以内；代码只记录相对路径、逻辑标识、类型或函数名。

出现以下任一情况，继续工作前更新 `PROGRESS.md`：

1. 方案、范围、协议或技术路线改变；
2. 完成主要阶段或可验收交付物；
3. 输入材料、版本或用户要求实质变化；
4. 出现失败、风险、阻塞或需要用户决定的事项；
5. 准备暂停、切换会话，或上下文已明显变长；
6. 当前任务完成。

## 4. 路径和外部资源（强制）

- 仓库内文档、配置、源码只写相对路径，例如 `core/device/`。
- 仓库外参考源码使用 `docs/references/source-map.md` 中的逻辑标识，例如 `REF_TCODE_FIRMWARE`。
- 禁止把开发机盘符、用户名、IP、COM 端口或密码写入源码、测试、日志、Gradle 配置和进度文件。
- 本地环境差异写入未跟踪的 `local.properties` 或进程环境变量。

## 5. 写入与验证（强制）

- 新增或整文件重写时，先写同目录临时文件，命名为 `<name>.tmp.<task-id>`；完整读取确认后再用同文件系统重命名替换目标。
- 小范围源码修改使用 `apply_patch`；工具提供事务型替换时视为原子写入。
- 每次修改后重新读取最终目标的关键段；回复前再检查 `git diff --check` 和 `git status --short`。
- 声称“已保存”或“已完成”前，必须把最终文件重新读回。失败时明确写“未保存”，聊天回复不能替代持久化。
- 不用临时文件覆盖更晚的用户修改；替换前比较目标文件状态。

## 6. AI 任务粒度

- 一个会话只负责一个可独立验收的纵向模块或一个明确阶段，不同时铺开多个传输层或多个 UI 面。
- 常规任务控制在约 8 到 12 个主要源码文件；生成文件、资源和窄测试夹具不计入，但仍需列出。
- 任务开始时写清输入规格、允许修改目录、禁止修改目录、验收命令和退出条件。
- 若需要读取超过 3 个大型上游仓库、修改超过 12 个主要源码文件，或上下文开始影响判断，应先 checkpoint，再拆成后续任务。
- 不把“实现全部 UI”“实现全部设备连接”作为单个会话任务。按 `TASKS.md` 的阶段边界执行。

## 7. 凭据和隐私

- 不保存 SSH、SMB、Git、设备配对或签名凭据；不把凭据传给第三方页面或构建服务。
- 日志必须脱敏网络地址、用户名、媒体文件名和本地路径。
- 媒体索引、观看行为和推荐画像默认只保存在应用私有存储，不上传。
- 不将成人媒体名称、缩略图或脚本内容加入测试夹具；使用合成的中性数据。

## 8. 真实 Android 设备安全边界

- 测试设备逻辑标识为 `TEST_TABLET`；仅允许操作本项目包名、本应用私有目录和用户明确指定的测试媒体目录。
- 禁止读取、修改或删除 Android 系统敏感数据、其他应用数据、账户、系统设置和未授权媒体目录。
- 禁止恢复出厂、批量卸载/停用包、宽范围删除、修改 SELinux、重挂载系统分区或写入系统属性。
- 即使 `adb root` 可用，也使用完成当前测试所需的最低权限。
- 安装、清数据、卸载或写媒体前，先核对包名/目标路径；测试后仅清理本应用产生的内容。
- 详细命令策略见 `docs/security/test-device-policy.md`。

## 9. 真实 TCode 设备安全边界

- 设备逻辑标识为 `TEST_OSR_DEVICE`；未经用户明确授权，不烧录、不擦除固件、不改变持久配置。
- 首次联调必须先使用模拟传输或低幅限制；真实设备默认轴范围为安全测试值，逐步放开。
- 所有连接断开、播放停止、异常、前台服务销毁和应用退出路径都必须调用停止策略。
- 设备命令必须经过轴白名单、范围钳制、速率限制和过期丢弃，UI 不可绕过安全层。
- 不记录原始高频设备数据到长期日志。

## 10. Git 与参考仓库

- 本仓库是独立产品仓库，不把相邻上游源码仓库作为子目录提交。
- 不整理、恢复、覆盖或提交任何参考仓库中的现有改动。
- 未经请求不执行 destructive Git 操作，不改写历史。
- 引入源码前审查许可证和 NOTICE；复制的实现必须记录来源、版本和许可证。

## 11. 验证最低要求

- 文档：链接和相对路径有效，决策与 `PROGRESS.md` 一致。
- Kotlin：至少运行相关模块单元测试和 `assembleDebug`。
- 数据库：迁移测试、2,000+ 条合成媒体性能测试、排序确定性测试。
- 播放/Funscript：使用确定性时钟和录制命令流测试，不先依赖真实设备。
- 设备传输：先模拟器/loopback，再真实设备；断连和急停测试是验收必选项。
- UI：手机/平板、横竖屏、手势冲突、文本溢出和系统栏均需截图或自动化验证。

## 12. 长会话自保护

当历史很长、工具调用很多、响应变慢或出现请求失败时：

1. 立即把完整状态、决定、验证和下一步压缩写入 `PROGRESS.md`；
2. 重读确认保存成功；
3. 告知用户上下文已较长，建议新会话从 `PROGRESS.md` 接续。


## Skills
- 若需浏览器自动化操作，通过命令行`playwright-cli --help`进行自动化操作
- 若需搜索代码文档，通过命令行`ctx7 library`或`ctx7 docs`从源代码中提取最新的、特定版本的文档和代码示例
- 若需window端UI自动化测试，通常使用python第三方库`pywinauto`；长期回归重型测试则使用FlaUI SDK，允许自行配置SDK等。
