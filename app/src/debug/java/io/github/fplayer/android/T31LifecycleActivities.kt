package io.github.fplayer.android

/** Debug-only activities used to force full UI rebuilds without changing device settings. */
class T31PortraitActivity : MainActivity()

class T31LandscapeActivity : MainActivity()
