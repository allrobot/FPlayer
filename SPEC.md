# FPlayer 产品规格

## 1. 产品目标

在 Android 手机上和平板上提供适合大型本地媒体库的沉浸式播放器：视频像短视频 Feed 一样连续浏览，Funscript 与视频时钟同步，TCode 设备在前台或锁屏后台都能可靠运行，并能在任何异常路径安全停止。

目标媒体库规模为 2,000 个以上视频及脚本。核心体验必须在索引更新、SMB 延迟、缩略图缺失和设备断线时仍保持可操作。

## 2. 首期范围

### 2.1 媒体播放

- libmpv 播放内核，优先硬解，支持用户切换软解/硬解。
- 支持 AV1、VP9、H.264、H.265 及 libmpv/FFmpeg 构建实际启用的常见容器、音频和字幕格式。
- 上一项/当前项/下一项三槽播放器，预加载相邻媒体但仅当前项输出音频和设备命令。
- 单击播放/暂停，垂直分页切换，断点续播，无限循环和自动连播。
- 倍速 0.1x 到 5x；脚本同步必须使用媒体时钟，不能用墙钟独立推进。
- 自动匹配同名字幕，允许通过系统文件选择器覆盖当前字幕。

### 2.2 后台播放

- 用户显式开启后台播放后，Activity 不可见或屏幕关闭时继续媒体、循环脚本和设备输出。
- 使用 media playback 类型的 Foreground Service、MediaSession 和媒体通知。
- 通知至少提供播放/暂停、停止设备、退出后台播放；“停止设备”必须立即清空待发队列。
- 默认策略为音频随媒体继续播放；后续设置可单独静音，不通过把系统音量改为零实现。
- 进程终止、服务销毁、播放错误、连接丢失、用户停止和媒体队列耗尽均进入统一安全停止状态机。

### 2.3 Funscript

- 解析标准单轴 Funscript 的 `actions[].at` 和 `actions[].pos`，严格校验范围、排序和重复时间点。
- 识别多轴脚本命名并映射 TCode 轴；具体命名兼容表在实现任务中固定为测试向量。
- 同名匹配采用可预测的规范化规则，冲突时给出确定选择和可见诊断。
- 支持偏移、插值、每轴输出限制、命令提前量和速率限制。
- 无同名脚本时按用户选项：指定脚本、随机轴、0 到 100 循环或不输出。
- 热力图是脚本预览/进度控件，不是设备安全层；关闭后退化为细进度条。

### 2.4 TCode 设备

- 固件语义以 `REF_TCODE_FIRMWARE` 为首要参考，兼容 TCode 0.3，并为版本协商保留接口。
- 首期传输：TCP、UDP、WebSocket、BLE、经典蓝牙 BR/EDR SPP、USB 串口。
- Ayva/raw WebSocket 可作为桥接协议；不让 JavaScript 运行时成为核心设备依赖。
- 每轴可配置启用状态、最小/最大输出、反转、归中值和安全测试幅度。
- 所有命令经过范围钳制、单调时间戳、过期丢弃、发送速率限制和统一急停。
- Intiface Central、Handy/Handyplug 不属于首期；未来通过独立 transport/adapter 扩展。

### 2.5 大型媒体库与 SMB

- 本地目录使用 Storage Access Framework 持久授权；不依赖 root 或全盘扫描权限。
- SMB 凭据存放 Android Keystore 保护的本地存储，不写数据库明文、日志或导出文件。
- 扫描为可取消、可恢复的增量任务；元数据、脚本匹配和缩略图分优先级执行。
- 首屏先读取数据库快照，扫描不得阻塞首个可播放媒体。
- 删除陈旧缩略图只能在一次完整扫描成功提交后执行，扫描中断时保留旧缓存。
- 排序字段：标题、修改日期、最近播放时间、状态、时长、大小、分辨率、路径；全部支持升降序和稳定次级键。
- 首页与相册共享排序定义。排序变化保持当前媒体不变，只重定位它在新顺序中的索引。
- 为常用 scope + sort 组合维护带 generation 的物化顺序；内容改变时增量失效并后台重建。

### 2.6 UI 与导航

- 底部四个主入口：首页、相册、设备、设置。
- 首页顶部为透明工具带：抽屉、来源/集合切换和搜索；具体按钮数量以信息架构和窄屏适配验证为准，不强行挤压七个文字按钮。
- 播放页右侧是点赞、收藏、点踩等竖向操作；左下显示文件名、文件夹和媒体信息。
- 首页垂直 Feed 与网格/双列浏览互相切换；从网格选择媒体返回对应播放来源和顺序。
- 相册支持所有文件夹、含视频文件夹、全部视频三种视图，以及网格/双列布局。
- 长按打开竖屏底部半屏或横屏右侧半屏播放设置；清屏模式只保留退出、后退 5 秒、播放/暂停、倍速。
- 进度控件在播放时弱化、暂停时显示；拖动时增粗、扩大触摸热区、显示时间并抑制冲突手势。
- 详细状态机和手势仲裁见 `docs/ui/interaction-spec.md`。

### 2.7 纯算法推荐

- 不下载、训练或运行本地模型，不上传媒体库和行为数据。
- 候选评分使用可解释的内容/目录偏好、显式反馈、有效前台观看、跳过行为、最近性和新颖度。
- 推荐列表用 MMR 做多样性重排，并保留 5% 到 10% 未看内容探索位。
- 完成度只计算前台唯一观看覆盖；循环次数不直接等同完成度。
- 分别记录 `completionCount`、`manualReplayCount`、`foregroundLoopCount`、`backgroundLoopCount`；后台自动循环只给极低且饱和的偏好增益。
- 点踩是强负反馈；删除文件是独立操作，不能由点踩自动触发。

## 3. 非功能要求

- 2,000+ 媒体索引时，启动和 Feed 滑动不等待全库重新排序或缩略图生成。
- 设备调度不在主线程执行；高频命令队列有上限，不允许无限积压。
- 所有播放、索引和设备状态机可用确定性测试时钟验证。
- 手机和平板、横竖屏、触摸与系统手势边缘、刘海/系统栏均需覆盖。
- 用户数据默认离线，诊断导出前可预览并脱敏。

## 4. 首期明确排除

- Handy/Handyplug。
- Intiface 作为 OSR SPP 中介。
- 云端账户、跨设备同步、远程画像和本地机器学习模型。
- 未经用户选择的全盘媒体扫描。
- 后台无通知的隐形设备运行。
- 固件烧录器和固件持久配置编辑。

## 5. 发布门槛

- libmpv 许可模式、FFmpeg 构建选项和全部直接依赖通过许可证矩阵审查。
- 前后台切换、锁屏循环、断连、崩溃恢复和通知急停均通过测试。
- 真实平板测试只访问本应用数据；真实 TCode 测试从受限轴范围开始且不刷固件。
- 2,000+ 合成媒体库的扫描、排序、物化顺序和缩略图清理性能达到阶段基准。
